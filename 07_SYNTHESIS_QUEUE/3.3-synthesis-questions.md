# Module 3.3 Synthesis Questions

Source module: `02_RAW_MODULES/3.3-common-mistakes-and-how-to-fix-them.md`

## Questions

- Should RitmaOS include an automated workspace health checker?
- What should the first validation rules be?
- Should CLAUDE.md have a recommended line limit, token limit, or both?
- Should RitmaOS enforce a routing table, or only warn when missing?
- How should RitmaOS detect “too many workspaces” without being too rigid?
- Should context files include a required `Last updated` field?
- Should RitmaOS support context-staleness warnings?
- How should the product help users decide between a new workspace and a subfolder?
- Should the product include a “minimum viable workspace” mode by default?
- Can recurring user edits be used to suggest source-level context updates later?
- Should workspace validation be a CLI command, a UI feature, or both?
- Which 3.3 mistakes are MVP-critical and which can wait?
