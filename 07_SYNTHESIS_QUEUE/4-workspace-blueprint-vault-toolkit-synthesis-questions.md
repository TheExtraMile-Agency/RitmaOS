# Module 4 Synthesis Questions

Source module: Workspace Blueprint / Vault Toolkit Practical Asset Package

## Questions

- Should asset-heavy modules become their own module type in RitmaOS?
- Should `05_NEW_MODULES_INBOX/module-x/` become the canonical staging location for downloadable zips?
- Should extracted files include per-asset inventories?
- Should RitmaOS unpack asset packages into a preserved raw-assets directory, or keep zips intact only?
- How should downloadable examples be linked to framework extraction without contaminating raw material?
- Which Vault Toolkit constraints are durable enough to become core RitmaOS operating principles?
- Should RitmaOS support a package installer that excludes teaching-only or placeholder files by default?
- Should workspace blueprints become product templates only after a separate cleanup/review pass?
- How should nested repos inside downloadable packages be represented in inventory and checksums?
- Should raw overview files be required for every asset-heavy module?

## Synthesis Direction

Module 4 should be synthesized with Section 3 because it turns the folder architecture into distributable assets: blueprints, constraints, reference architectures, and builder skills.
